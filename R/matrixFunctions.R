count_characters<-function(fn,ln) 
{
    res<-paste(fn, ln)
    for(i in res)
    {
        cat("The total number of characters in ",i," is ",nchar(i),"\n") 
    }
}
first_name<-c("kandura", "sasuke", "prem")
last_name<-c("kumar", "faddds", "hemanth")
count_characters(first_name, last_name)
#' random select
 sample_n(tbl, size, replace, fac, …)

 first_name<- iris
sample_n(first_name,10)
#'function count character
count_characters <- function ( first_name, last_name){
res<- paste (first_name,last_name)
  score<-c\()
  for ( i in res) 
    {cat" the total number of children in"i" is " nchar(i,"\n")}
  characters 
  score<- c(score, nchar(i)
            vector}
            first_name-cl assification = c()
            for ( i in first_name{
              if (nchar(i) > 6) {
                result = "long"
                }
              else
                result = "short" }
                 
            first_name_classification [i] <-result
                max_value = max(score)                                           
    cat("Maximum value of vector ", score, "is ", max_value, "\n")   
    cat("The classification vector is ", fname_classification, "\n") 
}

first_name<-c("kandura", "sasuke", "prem")                               
last_name<-c("kumar", "faddds", "hemanth")                          
count_characters(first_name, last_name)
                 
                 
}
